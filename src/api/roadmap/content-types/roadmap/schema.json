{
  "kind": "collectionType",
  "collectionName": "roadmaps",
  "info": {
    "singularName": "roadmap",
    "pluralName": "roadmaps",
    "displayName": "Roadmap Milestone",
    "description": "Product roadmap milestones (upcoming and previous releases)"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "string"
    },
    "cover": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": [
        "images"
      ]
    },
    "summary": {
      "type": "richtext"
    },
    "releaseDate": {
      "type": "date",
      "description": "Use the first day of the release month (YYYY-MM-01).",
      "required": true
    },
    "githubUrl": {
      "type": "string",
      "required": false
    }
  }
}
